# cn_hints_bridge_forge_extension
Generate **Canny** + **OpenPose** hint maps (batch or single), preview them, optionally clean/edit Canny, then **send directly into ControlNet** (txt2img + img2img) inside **Forge / Forge Neo**.
